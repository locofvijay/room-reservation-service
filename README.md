# room-reservation-service
The Room Reservation Service is a Spring Boot application designed to handle hotel room reservations. It manages reservation requests, processes payments, and interacts with other services (such as a credit card payment API).
